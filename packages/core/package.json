{
  "name": "@carretao/core",
  "version": "0.1.0",
  "private": true,
  "description": "Regras de domínio do Carretão: comissão, máquinas de estado, atribuição (RN-*)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
